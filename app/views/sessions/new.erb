<h1>Log in!</h1>

<%= form_for(@user, url: sessions_path(@user)) do |f| %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :password %>
  <%= f.text_field :password %>

  <%= f.submit %>
<% end %>
